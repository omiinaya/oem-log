---
import { SITE_TITLE, AUTHOR_HANDLE } from '../consts';
import HeaderLink from './HeaderLink.astro';
---

<!-- prevent flash-of-wrong-theme: apply saved light theme before paint -->
<script is:inline>
	const saved = (() => {
		try { return localStorage.getItem('oem-log-theme'); } catch { return null; }
	})();
	if (saved === 'light') {
		document.documentElement.setAttribute('data-theme', 'light');
	}
</script>

<header>
	<nav>
		<a href={`${import.meta.env.BASE_URL}`} class="brand" aria-label="Home">
			<span class="brand-prompt" aria-hidden="true">$</span>
			<span class="brand-name">{SITE_TITLE}</span>
			<span class="cursor" aria-hidden="true"></span>
		</a>

		<div class="internal-links">
			<HeaderLink href={`${import.meta.env.BASE_URL}`}>home</HeaderLink>
			<HeaderLink href={`${import.meta.env.BASE_URL}blog/`}>notes</HeaderLink>
			<HeaderLink href={`${import.meta.env.BASE_URL}about/`}>about</HeaderLink>
		</div>

		<div class="controls">
			<button class="theme-toggle" type="button" aria-label="Toggle theme" title="Toggle theme">
				<span class="icon" aria-hidden="true"></span>
			</button>
			<a href={`https://github.com/${AUTHOR_HANDLE}`} target="_blank" rel="me" aria-label="GitHub">
				<span class="sr-only">GitHub</span>
				<svg viewBox="0 0 16 16" aria-hidden="true" width="18" height="18">
					<path fill="currentColor" d="M8 0C3.58 0 0 3.58 0 8c0 3.54 2.29 6.53 5.47 7.59.4.07.55-.17.55-.38 0-.19-.01-.82-.01-1.49-2.01.37-2.53-.49-2.69-.94-.09-.23-.48-.94-.82-1.13-.28-.15-.68-.52-.01-.53.63-.01 1.08.58 1.23.82.72 1.21 1.87.87 2.33.66.07-.52.28-.87.51-1.07-1.78-.2-3.64-.89-3.64-3.95 0-.87.31-1.59.82-2.15-.08-.2-.36-1.02.08-2.12 0 0 .67-.21 2.2.82.64-.18 1.32-.27 2-.27.68 0 1.36.09 2 .27 1.53-1.04 2.2-.82 2.2-.82.44 1.1.16 1.92.08 2.12.51.56.82 1.27.82 2.15 0 3.07-1.87 3.75-3.65 3.95.29.25.54.73.54 1.48 0 1.07-.01 1.93-.01 2.2 0 .21.15.46.55.38A8.012 8.012 0 0 0 16 8c0-4.42-3.58-8-8-8z"/>
				</svg>
			</a>
		</div>
	</nav>
</header>

<script>
	const toggle = document.querySelector('.theme-toggle');
	const icon = toggle && toggle.querySelector('.icon');
	function apply(theme) {
		document.documentElement.setAttribute('data-theme', theme);
		try { localStorage.setItem('oem-log-theme', theme); } catch {}
	}
	function paint() {
		if (!icon) return;
		const light = document.documentElement.getAttribute('data-theme') === 'light';
		icon.textContent = light ? '☾' : '☀';
	}
	if (toggle) {
		toggle.addEventListener('click', () => {
			const current = document.documentElement.getAttribute('data-theme');
			apply(current === 'light' ? 'dark' : 'light');
			paint();
		});
	}
	paint();
</script>

<style>
	header {
		position: sticky;
		top: 0;
		z-index: 100;
		margin: 0;
		padding: 0 1.25rem;
		background: var(--header-bg);
		backdrop-filter: blur(8px);
		-webkit-backdrop-filter: blur(8px);
		border-bottom: 1px solid var(--line);
	}
	nav {
		max-width: var(--maxw);
		margin: 0 auto;
		display: flex;
		align-items: center;
		justify-content: space-between;
		gap: 1rem;
		height: 60px;
	}
	.brand {
		display: flex;
		align-items: center;
		gap: 0.5rem;
		text-decoration: none;
		color: var(--ink);
		font-family: var(--font-mono);
		font-size: 0.95rem;
	}
	.brand-prompt { color: var(--ink-faint); }
	.brand-name { font-weight: 700; color: var(--ink); }
	.brand:hover .brand-name { color: var(--accent-bright); }
	.cursor {
		display: inline-block;
		width: 0.55em;
		height: 1em;
		background: var(--ink);
		margin-left: 0.1em;
		animation: blink 1.2s step-end infinite;
	}
	@keyframes blink { 0%, 100% { opacity: 1; } 50% { opacity: 0; } }
	.internal-links { display: flex; align-items: center; }
	.internal-links :global(a) {
		font-family: var(--font-mono);
		font-size: 0.8rem;
		color: var(--ink-dim);
		padding: 0 0.7rem;
		border-left: 1px solid var(--line);
		text-decoration: none;
		transition: color 0.12s ease;
	}
	.internal-links :global(a:first-child) { border-left: none; }
	.internal-links :global(a:hover) { color: var(--ink); }
	.internal-links :global(a.nav-active) {
		color: var(--ink);
		font-weight: 700;
		text-decoration: underline;
		text-underline-offset: 5px;
		text-decoration-color: var(--ink-faint);
	}
	.controls { display: flex; align-items: center; gap: 0.85rem; }
	.theme-toggle {
		display: inline-flex;
		align-items: center;
		justify-content: center;
		width: 32px;
		height: 32px;
		border: 1px solid var(--line);
		border-radius: 6px;
		background: transparent;
		color: var(--ink-dim);
		cursor: pointer;
		font-size: 0.95rem;
		transition: color 0.12s ease, border-color 0.12s ease, background 0.12s ease;
	}
	.theme-toggle:hover { color: var(--ink); border-color: var(--ink-dim); background: var(--bg-2); }
	.controls a { display: flex; align-items: center; color: var(--ink-faint); transition: color 0.12s ease; }
	.controls a:hover { color: var(--ink); }
</style>